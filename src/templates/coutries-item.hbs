{{#each this}}
  <li class="country-list__item" data-name="{{name}}">
    <img class="country-list__img" src="{{flags.svg}}" alt="{{name}}" width="30"></img>
    <span class="country-list__text">{{name}}</span>
  </li>
{{/each}}
